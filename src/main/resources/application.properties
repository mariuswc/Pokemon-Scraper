# Default Spring Boot config
# Use a fixed port in development so it's easy to connect.
server.port=8081

# If you don't use Mustache templates yet, disable template-location checks to avoid warnings on startup.
spring.mustache.check-template-location=false

# Basic logging defaults
logging.level.root=INFO
logging.level.org.example=INFO
